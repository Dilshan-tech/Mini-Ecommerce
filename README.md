# Mini-Ecommerce
This is a mini e-commerce backend built with Express.js, using file handling instead of a database. It supports adding products, viewing them, fetching by ID, placing orders (stored in files), and serving static product images, with proper error handling and stream support for large files.
